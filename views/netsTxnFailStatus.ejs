<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transaction Failed</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fraunces:wght@600;700&family=Source+Sans+3:wght@400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-1: #fff6f6;
            --bg-2: #f8fbff;
            --card: #ffffff;
            --ink: #1b1f2a;
            --muted: #6b7280;
            --accent: #b42318;
            --accent-2: #f04438;
            --ring: rgba(240, 68, 56, 0.18);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            min-height: 100vh;
            display: grid;
            place-items: center;
            padding: 32px 16px;
            font-family: "Source Sans 3", "Segoe UI", Tahoma, Arial, sans-serif;
            color: var(--ink);
            background: radial-gradient(1200px 600px at 10% 10%, var(--bg-1), transparent 55%),
                radial-gradient(1200px 600px at 90% 0%, var(--bg-2), transparent 55%),
                #f9fafb;
        }

        .card {
            width: min(720px, 100%);
            background: var(--card);
            border-radius: 20px;
            padding: 28px;
            box-shadow: 0 16px 50px rgba(15, 23, 42, 0.12);
            border: 1px solid #f0f2f5;
        }

        .header {
            display: flex;
            gap: 16px;
            align-items: center;
            margin-bottom: 12px;
        }

        .icon-wrap {
            width: 64px;
            height: 64px;
            border-radius: 16px;
            background: linear-gradient(135deg, #ffe4e6, #fff1f2);
            display: grid;
            place-items: center;
            border: 1px solid #fee2e2;
            box-shadow: 0 0 0 6px var(--ring);
        }

        .icon-wrap img {
            width: 34px;
            height: 34px;
        }

        h1 {
            margin: 0;
            font-family: "Fraunces", "Times New Roman", serif;
            font-size: clamp(28px, 3vw, 36px);
            letter-spacing: 0.2px;
        }

        .subtitle {
            margin: 8px 0 20px;
            color: var(--muted);
            font-size: 16px;
        }

        .pill {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 6px 12px;
            border-radius: 999px;
            background: #fff1f0;
            color: var(--accent);
            font-weight: 600;
            font-size: 13px;
            letter-spacing: 0.4px;
            text-transform: uppercase;
        }

        .actions {
            margin-top: 20px;
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
        }

        .btn {
            padding: 10px 16px;
            border-radius: 10px;
            border: 1px solid transparent;
            font-weight: 600;
            text-decoration: none;
            transition: transform 0.12s ease, box-shadow 0.12s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--accent), var(--accent-2));
            color: #fff;
            box-shadow: 0 10px 20px rgba(240, 68, 56, 0.25);
        }

        .btn-secondary {
            background: #fff;
            color: var(--ink);
            border-color: #e5e7eb;
        }

        .btn:hover {
            transform: translateY(-1px);
        }

        .note {
            margin-top: 16px;
            font-size: 13px;
            color: var(--muted);
        }

        @media (max-width: 480px) {
            .header {
                flex-direction: column;
                align-items: flex-start;
            }

            .icon-wrap {
                width: 56px;
                height: 56px;
            }
        }
    </style>
</head>

<body>
    <div class="card">
        <div class="header">
            <div class="icon-wrap">
                <img src="/images/redCross.png" alt="Transaction failed icon">
            </div>
            <div>
                <span class="pill">Payment Failed</span>
                <h1><%= message %></h1>
            </div>
        </div>
        <p class="subtitle">Please try again. If the issue persists, verify your payment details or try a different method.</p>
        <div class="actions">
            <% if (typeof invoiceId !== 'undefined' && invoiceId) { %>
                <a class="btn btn-primary" href="/payment/retry/<%= invoiceId %>">Try Again</a>
            <% } else { %>
                <a class="btn btn-primary" href="/payment">Try Again</a>
            <% } %>
            <a class="btn btn-secondary" href="/">Back to Home</a>
        </div>
        <div class="note">If you already scanned the QR, wait a few seconds and refresh the status.</div>
    </div>
</body>

</html>
